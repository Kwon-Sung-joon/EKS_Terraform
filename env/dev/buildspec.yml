version: 0.2
phases:
  pre_build:
    commands:
      - pwd
      - ls -al
      - cd ./env/dev/
  build:
    commands:
      - terraform --version
      - terraform init
      - terraform apply -target module.subnets --auto-approve
      - terraform apply --auto-approve